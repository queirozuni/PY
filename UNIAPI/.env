# Gere um novo segredo com:
# python -c "import secrets;print(secrets.token_urlsafe(64))"

SECRET_KEY="QCeyDz-qVlJitjvKSIF2-HO4OhdmQAIMLndS75JezegDGg8rejW0YQA4zQJTfb21pW707hvJ8hdFOPhEt5RqLA"

# Banco de dados (SQLite para começar; em produção prefira Postgres)
DATABASE_URL="sqlite+aiosqlite:///./data/app.db"

# Usuário inicial (criado automaticamente se não existir)
ADMIN_USER="admin"
ADMIN_PASS="Unimed@236"  # será hasheada em Argon2id no primeiro run

# Domínios permitidos no CORS (separe por vírgula)
ALLOWED_ORIGINS="https://seu-dominio.com,http://localhost:8000"
